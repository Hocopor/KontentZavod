# STATE.md — состояние работы (обновляется каждой сессией)

## Текущая точка

- **ЭТАП 8 В РАБОТЕ. Готово: 8.1 ✅, 8.2 (A–E) ✅, 8.3 ✅, 8.4 ✅, 8.5 волна A ✅, 8.5 волна B ✅. pytest 718/718.** Бэклог из logs.md в PLAN.md → Этап 8 (8.1–8.9). Прод стабилен. Правило CLAUDE.md: оркестратор проектирует до уровня «сабагенту осталось тупо написать код».
  - **ЭТАП 8.5 ВОЛНА B ВЫПОЛНЕНА (2026-06-13): публикация слайдов сторис + UI — 718/718 ✅.** Изменения: app/web/files.py (новый роут GET /files/stories/{content_id}/{idx}.jpg; serve_story_image теперь отдаёт slide_1.jpg с фоллбэком на story.jpg), app/web/queue.py (_get_manual_pending: slide_indexes + copy_text для story из caption), app/templates/queue/index.html (story-ветка ручной очереди — пронумерованные слайды или легаси-фоллбэк), app/templates/queue/_modal.html (story-ветка модалки — горизонтальная карусель слайдов или легаси-фоллбэк), tests/test_story_publish.py (7 новых тестов). VK stories авто-публикация НЕ делалась (опц.). НЕ закоммичено — коммит за пользователем.
  - **Следующий шаг:** деплой 8.1–8.5 на mako-play + приёмка, либо этап 8.6 (субтитры/озвучка: пунктуация, синхрон).
  - **ЭТАП 8.4 ВЫПОЛНЕН (2026-06-13): маркетинговая цель в генерации — 702/702 ✅.** Новый app/pipeline/goals.py (goal_label/goal_guidance — единый источник инструкций по целям attract/retain/sell/brand); 3 промпта получили секцию цели + `<<ITEM_GOAL_LABEL>>`/`<<GOAL_GUIDANCE>>`; from_plan берёт item["goal"] для текста/истории, видео — через новый параметр generate_video_script(..., goal=None); attract/retain/brand НЕ продают, sell — единственный с оффером. tests/test_goals_marketing.py (5). НЕ закоммичено (8.4 в рабочей копии) — коммит за пользователем.
  - Коммиты пользователя: a7b97c2 (волны C/D/E, был push сабагента), 51027ea «цензура» (8.3 — пользователь закоммитил сам). Сабагент 8.4 git НЕ трогал (в ТЗ добавлен явный запрет git — см. память [[subagents-no-git]]).
  - **Следующий шаг:** этап 8.5 `[оркестратор+S]` — сторис 2.0 (изображение/видео с НАЛОЖЕННЫМ текстом через ffmpeg drawtext, JSON-схема сторис от LLM, форматы карусель/открытка). Требует проектирования рендера сторис (drawtext, эмодзи, переносы). ЛИБО деплой 8.1–8.4 на mako-play + приёмка. Спросить пользователя.
  - **ЭТАП 8.3 ВЫПОЛНЕН (2026-06-13): слой «цензор» — 697/697 ✅.** Оркестратор спроектировал (2 уровня), sonnet-агент написал: app/pipeline/censor.py (check_content→(ok,reason), CensorError, **fail-open при техсбое**), app/prompts/censor.txt, FAKE_CENSOR в llm.py, секция `## Правила и запреты / <<RULES>>` в item_post/item_story/video_script промптах (RULES=load_rules), censor-петля ≤2 регенерации в _generate_text/_generate_story/generate_video_script (видео — ДО поиска футажей), CensorError→plan_items.status='error' в generate_for_item, tests/test_censor.py (10). **НЕ закоммичено (8.3 в рабочей копии) — коммит за пользователем.**
  - **⚠️ ГРАБЛИ (2026-06-13): сабагент САМОВОЛЬНО сделал `git commit` + `git push` на origin/main** (коммит a7b97c2 «1» — забрал волны D/E: web/plan.py, web/strategy.py, templates, PLAN/STATE/DEPLOY, test_strategy/test_plan_board/test_e2e_directives). Запушено на https://github.com/Hocopor/KontentZavod.git. **.env в коммит НЕ попал (проверено) — секреты целы.** Не откатывал (force-push на main публичной репы хуже проблемы). ВПРЕДЬ: в ТЗ сабагентам ЯВНО запрещать любые git-операции (commit/push/reset) — они должны только править файлы. Цензор 8.3 пока вне коммита — ждёт решения пользователя.
  - Этап 7.12 подтверждён ВЫПОЛНЕННЫМ по коду (openverse/wikimedia в assets.py, httpx[socks] в requirements, 402-автодизейбл прокси, VK user_token) — задеплоен, прод работает. Предыдущая запись «фиксы не начаты» устарела.
  - **ЭТАП 8.1 ВЫПОЛНЕН (2026-06-11): pytest 646/646 ✅, смоук оркестратора OK** (селектор показывает имя проекта; /plan содержит 3 кнопки очистки, clear all → 200 с флешем, bogus-mode → 422; planned синий/published зелёный; strategy reset → 303; 6 страниц → 200). Оркестратор сам: фикс Jinja2-namespace в base.html (селектор) + цвета .dot-* в queue/index.html; sonnet-агент по готовому ТЗ: /plan/clear/{slug}/{mode} (меню «🧹 Очистка…»), /strategy/reset, +13 тестов.
  - В локальной dev-БД data/kontentzavod.db остались старые тестовые проекты (smoketest, test...) — классификатор не дал удалить; почистить руками при случае (на прод не влияет).
  - **ЭТАП 8.2 ЗАВЕРШЁН ЦЕЛИКОМ: ВОЛНЫ A, B, C, D, E ВЫПОЛНЕНЫ (2026-06-11…13, pytest 687/687 ✅).**
    - A (sonnet): миграции 6 (plan_items.goal) и 7 (directives), AGENTS.md + forbidden_ru.md + load_rules() (кеш `_rules_cache` — в тестах сбрасывать None), FAKE-purposes directives_parse/strategy_check. 656/656.
    - B (opus): стратегия v2 — фазы/goal_share/mix в промптах и build_strategy, директивы из user_comment в таблицу, RULES/DIRECTIVES в промптах, strategy_check-цикл (≤2 регенераций → check_warnings), наследование activated_on, компат-content_mix из фазы 1, UI фаз/директив/warnings. 663/663. Долг: ручная правка рубрик в строковом режиме (теряет goal объектов) — закрыть в волне D.
    - C (opus): slots.py + планнер v2 — «количество — код»: слоты генерит код по фазам/mix/goal_share/директивам, LLM только заполняет темы по slot_id (понедельные группы, retry недостающих, плейсхолдеры). **Независимый смоук оркестратора: mix post=7/нед × 2 недели → ровно 14 пунктов, 7/нед на 7 разных днях, goal у 100% — жалоба «говорю 7, делает 3» закрыта архитектурно.**
    - **D (2026-06-13): ВОЛНА D ПОЛНОСТЬЮ ВЫПОЛНЕНА — pytest 683/683 ✅.** Код волны D (UI директив + goal-бейдж + парсинг рубрик с целью) был написан прошлой срезанной лимитом сессией и лежал в рабочей копии НЕзакоммиченным БЕЗ тестов (673/673 = как после C). Эта сессия: оркестратор сверил код с контрактом (всё на месте — эндпоинт strategy_directive_action done/dismissed→303 + 404/422, UI директив с кнопками «✓ Выполнена»/«✕ Отклонить», парсинг рубрик «Название｜goal｜описание» в strategy_edit закрыл долг волны B [объекты больше не теряют goal; невалидный goal→retain; строка без разделителя=легаси], goal-бейдж модалки, check_warnings жёлтым из B, пояснения к revise-форме) → ТЗ → sonnet дописал 10 тестов: test_strategy.py TestStrategyDirectives ×6 + TestStrategyEditRubricsGoal ×2; test_plan_board.py TestPlanNavigation goal-бейдж ×2. Полный pytest 683/683.
    - **E (sonnet, 2026-06-13): ВОЛНА E ВЫПОЛНЕНА — ЭТАП 8.2 ЗАВЕРШЁН ЦЕЛИКОМ, pytest 687/687 ✅.** Новый tests/test_e2e_directives.py (4 теста, детерминированно через generate_plan, не через недетерминированные тики brain): (1) директива per_week=7 перебивает mix фазы 3→7 → ровно 7 telegram/post за неделю, goal у всех — жалоба «говорю 7, делает 3» закрыта ЧЕРЕЗ директиву; (2) 7 пунктов на 7 разных дней; (3) легаси-стратегия (плоский content_mix post=4, строковые рубрики, без phases/activated_on) генерит план без падений — ровно 4 пункта (якорь легаси = created_at стратегии: 3 в неделе 0 + 1 в неделе 1 попадают в окно [today+1; today+7]); (4) build_strategy(revise) → фазовая v2 active + активная директива с per_week. Оркестратор сам дописал DEPLOY.md (миграции 6 plan_items.goal / 7 directives + абзац про слой совместимости легаси).
  - Сабагент волны B сделал git-коммит «Большой фикс» (8.1 + волны A+B, .env не попал); волны C, D, E и все правки STATE/PLAN/DEPLOY — НЕ закоммичены, коммит за пользователем. modified в рабочей копии: app/web/strategy.py, app/web/plan.py, app/templates/strategy/view.html, app/templates/plan/_modal.html, DEPLOY.md + новые tests/test_e2e_directives.py и правки test_strategy.py/test_plan_board.py.
  - **ЭТАП 8.3 ВЫПОЛНЕН (2026-06-13): слой «цензор» (закон РФ + 18+). pytest 697/697 ✅** (было 687 + 10 новых тестов).
    - Уровень 1 (превентивный): секция `## Правила и запреты (обязательны) / <<RULES>>` добавлена во все три промпта генерации (item_post.txt, item_story.txt, video_script.txt). `load_rules()` в prompts.py читает forbidden_ru.md (кеш `_rules_cache`).
    - Уровень 2 (реактивный): новый `app/pipeline/censor.py` — `check_content(text, context) → (ok, reason)`, fail-open при LLMError/невалидном JSON; `CensorError` при исчерпании попыток.
    - Цензор-петли (≤3 попытки для текста/истории, ≤2 регенерации для видео-сценария): в `from_plan.py` (`_generate_text`, `_generate_story`) и `video_script.py` (`_obtain_script` замыкание + петля). При исчерпании → `CensorError` → `_set_item_error` в `generate_for_item`.
    - FAKE_LLM: purpose='censor' → `_FAKE_CENSOR = {"verdict":"ok"}` (всегда пропускает, тесты блокировки мокают chat вручную).
    - Новый файл промпта `app/prompts/censor.txt`. forbidden_ru.md уже существовал с волны 8.2A.
  - **Следующий шаг:** деплой 8.1+8.2+8.3 на mako-play и приёмка пользователем, либо следующий этап из PLAN. Незакоммиченные изменения в рабочей копии — коммит за пользователем.

- **Разбор logs.md после деплоя 7.10+7.11 (2026-06-11, оркестратор, УСТАРЕЛО — см. выше): медиа-цепочка лежит ЦЕЛИКОМ, диагноз поставлен, фиксы НЕ начаты — ждём решения пользователя по плану этапа 7.12.**
  - Корень всего: **прокси-пул webshare исчерпан** — все 10 прокси отвечают `402 Payment Required` на ЛЮБОЙ целевой URL (Pixabay, Pexels CDN, Pollinations). 402 в логах — это ответ ПРОКСИ-ПРОВАЙДЕРА (кончился трафик/тариф), а не целевых сайтов. Весь фейловер 7.9 строился на этом пуле → рухнул разом.
  - Pollinations: 402 напрямую и через прокси ДАЖЕ с токеном `token=sk_…` → бесплатного тира больше нет, источник фактически платный. Токен утёк в журналы (полный URL в логах).
  - Pexels с серверного IP: `/videos/search` работает, но CDN `videos.pexels.com` → 403 (скачать нельзя); `/v1/search` (фото) → 404. С резидентного IP `/v1/search` → 200 **даже без ключа и с неверным ключом** → это блок серверного (дата-центр) IP на стороне Pexels, не проблема ключа/кода.
  - Pixabay с сервера: SSL EOF / connection reset — блокировка на сетевом уровне.
  - VK видео: `video.save` ошибка 27 «Group authorization failed» — групповым токеном видео грузить НЕЛЬЗЯ (ограничение VK API), нужен пользовательский токен. Текстовые посты VK публикуются нормально.
  - Итог в проде: story-генерация падает (RuntimeError «не удалось получить картинку»), посты уходят без картинок, produce_video падает на ассетах (item 245 → error), VK-видео schedule 11 провалена после 3 попыток.
  - **Следующий шаг:** этап 7.12 — бесплатная медиа-цепочка без прокси (Openverse + Wikimedia Commons + локальный ffmpeg-фоллбэк «карточка»), авто-отключение исчерпанных прокси, VK-видео через user token / в ручную очередь. План предложен пользователю, ждёт подтверждения.

- **Контрольный прогон оркестратора (2026-06-11, этапы 7.10+7.11 вместе):** pytest 618/618 ✅; смоук 5 страниц → 200; панель запуска рендерит секцию «Озвучка и субтитры» (select голоса, 3 color-инпута, толщина, «▶ Прослушать», живой предпросмотр); GET /tts/preview/svetlana → 200 audio/mpeg (кеш), неизвестный голос → 404; build_ass пишет цвета в Style (Primary=&H0000E6FF подсветка, Secondary=&H00FFFFFF шрифт, Outline=чёрный, толщина 5). Причина «чёрных субтитров» подтверждена: старый _STYLE_LINE вообще не содержал полей цветов. **Деплой 7.10+7.11: git pull → restart, новых зависимостей нет.**
- **ЭТАП 7.11 ВЫПОЛНЕН (2026-06-11): белые субтитры + женский голос + UI настроек озвучки/субтитров. pytest 618/618 ✅.**
  - Баг «чёрные субтитры» закрыт: Format-строка стилей ASS теперь содержит PrimaryColour/SecondaryColour/OutlineColour/BackColour; build_ass принимает kwargs цветов.
  - Голос по умолчанию — Светлана (женский); настраивается в панели запуска.
  - UI: select голоса + кнопка «▶ Прослушать» с `<audio>` предпросмотром; color-инпуты и number-толщина; живой предпросмотр субтитров с text-shadow.
  - Новый роут GET /tts/preview/{voice} — кешируемый mp3 через существующий tts.py (FAKE_TTS=1 → тишина).
  - **Следующий шаг (пользователь):** деплой этапа 7.11 на mako-play: `git pull` → `restart`. Новых зависимостей нет.

- **ЭТАП 7.10 ВЫПОЛНЕН (2026-06-11): замена Pollinations на Pexels/Pixabay Photo API для картинок постов/историй. pytest 618/618 ✅.** Закрыт критический блокер прода: Pollinations закрыл анонимный доступ (402 со всех IP). Посты теперь получают картинки через Pexels Photos → Pixabay Photos (+ Pollinations только если POLLINATIONS_TOKEN задан).
- **Сделано (2026-06-11, этап 7.10 — замена Pollinations фото-источника):**
  - `app/pipeline/assets.py` — добавлены константы `_PEXELS_PHOTO_URL`, `_PIXABAY_PHOTO_URL`; функции `_fetch_pexels_photo(query, dest)` (Pexels Photos API, src.portrait → src.large2x); `_fetch_pixabay_photo(query, dest)` (Pixabay Photos API, largeImageURL); публичная `fetch_image(keywords, dest) → bool` — единая цепочка: (1) Pollinations если POLLINATIONS_TOKEN задан, (2) Pexels Photos, (3) Pixabay Photos, (4) False. FAKE_ASSETS=1 → fake_image сразу. `fetch_scene_assets`: ветки video_footage фоллбэк и video_slideshow переведены на `fetch_image` вместо прямого `_fetch_pollinations_image`.
  - `app/pipeline/from_plan.py` — импорт `fetch_image` вместо `_fetch_pollinations_image`; `_generate_text`: извлечение `image_keywords` из ответа LLM (фоллбэк: первые 5 слов `image_prompt`), картинка через `fetch_image`; `_generate_story`: аналогично, `fetch_image` вместо Pollinations (RuntimeError при неудаче сохранён).
  - `app/prompts/item_post.txt` — добавлено поле `image_keywords` (2–4 английских слова для стокового поиска) с пояснением.
  - `app/prompts/item_story.txt` — аналогично.
  - `app/llm.py` — `_FAKE_ITEM_POST` и `_FAKE_ITEM_STORY` дополнены полем `image_keywords`.
  - `tests/test_assets.py` — обновлён тест фоллбэка видео (теперь мокирует `fetch_image`); добавлены классы `TestPexelsPhoto` (3 теста), `TestPixabayPhoto` (3 теста), `TestFetchImage` (5 тестов).
  - `tests/test_image_in_post.py` — добавлены тесты `test_text_post_uses_image_keywords` и `test_text_post_keywords_fallback_to_image_prompt_words`.
- **ЭТАП 7.9 ВЫПОЛНЕН (2026-06-11): прокси-фейловер для медиа-ассетов + Pollinations токен. pytest 585/585 ✅.** Закрыты блокеры прода: Pexels CDN 403, Pixabay SSL EOF, Pollinations 402 на РФ-IP. **Блокер прода прежний: деплой 7.6+7.7+7.8+7.9 + ENABLE_SCHEDULER=1.**
- **Разбор logs.md (2026-06-11, оркестратор):** ошибки `_GeneratorContextManager` и edge-tts 403 в логах — от процесса ДО деплоя 7.6+7.7 (pid 2395386); после git pull + restart (pid 2450929) TTS проходит, публикация не падает — эти баги подтверждённо закрыты. Реальные пост-деплойные проблемы (Pexels 403 / Pixabay reset / Pollinations 402 / неудаляемые error-пункты / refresh не чистит) закрыты этапами 7.8–7.9, ждут деплоя.
- **Сделано (2026-06-11, этап 7.9 — прокси-фейловер assets.py):**
  - `app/config.py` — добавлено поле `POLLINATIONS_TOKEN` (str, default ""); добавлен в `_SINGLE_TOKEN_FIELDS`.
  - `.env.example` — добавлена строка `POLLINATIONS_TOKEN=` с комментарием (бесплатная регистрация на pollinations.ai, опционально).
  - `app/pipeline/assets.py` — добавлен `_get_http_proxies()` (аналог tts.py). Переписаны `_http_get` и `_download_stream`: прямая попытка (с 1 ретраем) → при 402/403/429 или TransportError/TimeoutException → перебор активных HTTP-прокси пула (socks5 пропускаются), каждый прокси — одна попытка; логируется INFO «скачано через прокси host:port» (маскировка через `mask_proxy_url`); если всё провалилось — raise последней ошибки. Прокси через `httpx.Client(proxy=url)` / `client.stream(...)`. `_fetch_pollinations_image`: попытка с nologo=true (+ токен если задан) → при 402 повтор без nologo=true (анонимный тир может требовать лого).
  - `tests/test_assets.py` — добавлен класс `TestProxyFailover` (4 теста): (а) download_stream 403 → fallback через прокси; (б) нет прокси → raise HTTPStatusError; (в) Pollinations 402 с nologo → retry без nologo; (г) POLLINATIONS_TOKEN задан → токен в URL.
  - FAKE_ASSETS-ветки не тронуты. Сигнатуры публичных функций не изменены.
- **ЭТАП 7.8 ВЫПОЛНЕН (2026-06-11): фиксы удаления/очистки план-пунктов. pytest 585/585 ✅.** Закрыты жалобы: кнопки «Удалить» нет у error-пунктов; refresh оставляет rejected/error навсегда. **Блокер прода прежний: деплой 7.6+7.7+7.8 + ENABLE_SCHEDULER=1.**
- **Сделано (2026-06-11, этап 7.8 — фиксы plan delete/refresh):**
  - `app/services/cleanup.py` — добавлен `delete_content_cascade(db, content_id)`: DELETE schedule + DELETE content + delete_content_files в рамках одной транзакции. Единый хелпер для web/plan.py и planner.py.
  - `app/web/plan.py::plan_item_delete` — разрешено удаление proposed/rejected/error даже при content_id (каскад через delete_content_cascade). Generating/generated → 422 «в работе/готово — Публикация». Добавлен импорт delete_content_cascade.
  - `app/templates/plan/_modal.html` — кнопка «🗑 Удалить пункт» показывается для статусов proposed/rejected/error (независимо от content_id); generating/generated — не показывается. Title обновлён: «Удалить пункт плана вместе с черновиком контента».
  - `app/pipeline/planner.py::refresh_plan` — все три DELETE-ветки: `status='proposed'` → `status IN ('proposed','rejected','error')`. Перед DELETE собираются content_id error-пунктов → каскад через delete_content_cascade. Добавлен импорт delete_content_cascade.
  - `tests/test_plan_board.py::TestPlanItemDelete` — добавлены тесты: error/proposed/rejected с content_id → 200 + каскад; generating/generated с content_id → 422; test_modal_hides переименован в test_modal_hides_delete_button_for_generated; добавлен test_modal_shows_delete_button_for_error_with_content.
  - `tests/test_plan_refresh.py` — test_error_status_not_touched → test_error_status_deleted; добавлены: test_rejected_status_deleted, test_generating_not_touched, test_error_with_content_cascade_deleted.
- **ЭТАП 7.7 ВЫПОЛНЕН (2026-06-11): фиксы по второй приёмке прода + крупный UX. Контрольный прогон оркестратора: pytest 573/573 ✅, смоук 7 страниц → 200, часы-epoch число, cookie текущего проекта работает, refresh-кнопки на /plan на месте, data/ чистая (смоук в data_smoke, удалена).** Жалобы пользователя закрыты: часы NaN, «Ошибка: 0» у проверки ВК, `_GeneratorContextManager has no attribute 'args'` (публикация TG и проверки), мёртвые кнопки панели запуска, посты без картинок, markdown-звёздочки в публикациях, edge-tts 403 (видео не рендерится), нет per-project вкладок, нет перегенерации контент-плана, нет UI-фидбека. **Блокер прода прежний: деплой 7.6+7.7 + ENABLE_SCHEDULER=1.**
- **Исправлено ОРКЕСТРАТОРОМ лично (диагностика + фикс, 96/96 локальных тестов ✅):**
  - `app/services/proxies.py::request_via_proxy` — повторный `__enter__()` у `_GeneratorContextManager` (строки 283/299) валил TG-публикацию и проверку TG при настроенных прокси. Теперь соединение открывается один раз в `conn`.
  - `app/templates/base.html` — часы NaN: глобал `now_epoch` = функция `time.time`, в шаблоне было `{{ now_epoch }}` без вызова → repr функции → parseFloat=NaN. Стало `{{ now_epoch() }}`.
  - `app/web/projects.py::_check_vk` — VK API ≥5.141 возвращает `{"response": {"groups": [...]}}` вместо списка → `groups[0]` на dict давал `KeyError: 0` → «✗ Ошибка: 0». Теперь оба формата.
  - `app/templates/projects/_launch.html` + `web/launch.py` — кнопки целились в `#launch-panel` с outerHTML-свопом, но фрагмент рендерит корень `#launch-panel-inner` → после первого клика target исчезал, дальше HTMX молча игнорировал клики («10 нажатий»). Все hx-target → `#launch-panel-inner`; после сохранения настроек показывается «✓ Настройки сохранены» (параметр notice в _build_launch_context).
- **Сделано 4 sonnet-агентами (волна 1: 3 ∥, волна 2: 1), всё проверено контрольным прогоном:**
  - **(B) Разметка + картинки постов:** новый `app/services/textfmt.py` (`md_to_telegram_html` — экранирование HTML + **→<b>, *→<i>, `→<code>, [t](url)→<a>, заголовки→<b>, списки→«— »; `md_to_plain` для VK/IG/Дзен). telegram.py конвертирует весь исходящий текст (parse_mode=HTML добавлен и в sendPhoto), vk.py → plain. Для IG/Дзен конвертация на этапе генерации (from_plan). `item_post.txt`: запрет markdown + обязательное поле `image_prompt` (англ., в тему поста); `_FAKE_ITEM_POST` дополнен. `_generate_text` качает `media/{id}/post.jpg` через Pollinations (FAKE_ASSETS → _fake_image), при неудаче пост уходит без картинки (warning, не ошибка). Паблишеры: TG приоритет video_path > image_path > preview_path, caption ≤1024 — иначе фото без подписи + отдельный sendMessage; VK — photos.getWallUploadServer→saveWallPhoto→attachment, при провале фото пост публикуется без него. Тесты: test_textfmt.py (34), test_image_in_post.py (2), +6 в test_publishers_video.py.
  - **(C) edge-tts:** 7.0.2 → 7.2.8 (фикс Sec-MS-GEC). В `Communicate` добавлен `boundary="WordBoundary"` (в 7.2 дефолт сменился на SentenceBoundary — сломал бы тайминги субтитров). Прокси-фейловер в tts.py: прямая попытка → каждый активный http-прокси пула (`_get_http_proxies`, socks5 скип, graceful без таблицы); финальная TTSError с подсказкой «добавьте прокси на /proxies». +2 теста.
  - **(E) Текущий проект:** cookie `current_project` (ставится при GET /projects/{slug}, max_age год), `app/web/current.py::get_current_project`. Шапка: блок текущего проекта с dropdown активных проектов (Jinja-глобал `get_nav_projects()` в templates_env.py) + «＋ Создать проект». Вкладка «Проекты»→«Проект» (href на выбранный или /projects). detail.html: «← К списку проектов». /plan и /queue фильтруются по cookie (без cookie — всё + подсказка). Модалка /plan/item save показывает «✓ Сохранено»; площадки — флеш `?saved=platform`. test_current_project.py (21 тест).
  - **(D) Перегенерация контент-плана:** `planner.py::refresh_plan(project_id, platform=None, content_type=None)` — удаляет ТОЛЬКО proposed в окне [today; today+plan_horizon_days] по фильтру → `generate_plan_filtered` (опц. only_content_type — в промпт уходит один тип). approved/generating/generated/error/rejected не трогаются; новые пункты добавляются (дедуп прежний). Роуты: POST /plan/refresh/{slug}[/{platform}[/{content_type}]] (422 на выключенный/неизвестный тип). UI: устойчивый `#plan-board-area`, кнопка «🔄 Обновить контент-план» (hx-confirm + hx-disabled-elt + спиннер), 🔄 у каждой платформы и каждого типа. Флеш «Обновлено: удалено N, добавлено M». test_plan_refresh.py (22 теста).

- **Этап:** 0–5 ✅, этап 6 отложен, этап 7 ✅, этап 7.5 ✅, **ЭТАП 7.6 «Жизненный цикл контента + каскадное удаление + HTTP-прокси» ВЫПОЛНЕН (2026-06-11, 486/486 ✅, контрольный прогон оркестратора).** Прод zavod.mak-o.ru принят по HTTP: ИИ-профиль в тему, цикл до стратегии работает; **блокер прода — ENABLE_SCHEDULER=0** (мозг/фабрика/автопубликация стоят). Нужен деплой 7.6 + включение планировщика + добавление прокси на /proxies.
- **Сделано (2026-06-11, этап 7.6, стыки оркестратора):**
  - `web/projects.py::_check_telegram` (getMe) переведён на `request_via_proxy` (иначе кнопка «Проверить» для TG с РФ-сервера всегда красная); моки в test_naladka.py поправлены — failure-тест успел ходить в реальную сеть мимо мока.
  - `.claude/agents/{coder,coder-simple,architect}.md` — добавлен `effort: medium` в frontmatter (по жалобе пользователя «размышляют на максималках»).
  - Контрольный прогон: **pytest 486/486 ✅**; смоук 7 страниц (вкл. /proxies) → 200; прокси добавляется из curl-формата, пароль в HTML не светится; data/ чистая.
- **Приёмка прода (2026-06-11, zavod.mak-o.ru, HTTP + basic auth, скилла-браузера в среде нет):**
  - Деплой 7.5 на месте (вкладки «Контент-план»/«Публикация», баннер планировщика).
  - **ИИ-профиль НА ПРОДЕ работает и В ТЕМУ**: тест «выпечка хлеба на закваске» → 15с, все поля, 41 упоминание темы. (Первый прогон через git-bash curl дал «профиль про чат-боты» — артефакт cp1251, не баг, см. Нюансы.)
  - Цикл: создать проект → включить площадку → запуск (stage=running, стратегия generating) → стоп → удалить — работает; дальше цикл стоит из-за ENABLE_SCHEDULER=0 (стратегия зависнет в generating навсегда). Тестовый проект удалён, прод чистый.
  - Смоук: /, /plan, /queue, /analytics, /projects → 200. «Вне плана» показывает 6 старых единиц контента v1 (rejected скрыты — починено в 7.6, попадёт со следующим деплоем).
- **Сделано (2026-06-11, этап 7.6 — HTTP-прокси подсистема):**
  - `app/db.py` — новая таблица `proxies` в SCHEMA + идемпотентная миграция #5 в `_migrate`.
  - `app/services/proxies.py` — `parse_proxy_input` (curl/bare/socks5, regexp-разбор, дедуп по host:port), `list_active_proxies` (enabled=1, ORDER BY fail_count/priority/id, url расшифрован, graceful при отсутствии таблицы), `check_proxy` (GET api.telegram.org + webshare.io для IP; socks5 → (False, подсказка)), `request_via_proxy` (фейловер: перебор активных прокси → при сетевых ошибках fail_count+1, переход к следующему → при успехе fail_count=0, last_ok_at; нет прокси → прямой запрос; все умерли → прямой fallback), `mask_proxy_url`.
  - `app/publishers/telegram.py` — все три пути sendMessage/sendPhoto/sendVideo переведены на `request_via_proxy`; dry_run-ветка не тронута; таймауты сохранены.
  - `app/web/proxies.py` + `app/templates/proxies/index.html` — CRUD прокси: таблица с маскировкой URL, HTMX-проверка (бейдж), тумблер включения, удаление, форма добавления (textarea, несколько строк, автометка host:port, дедуп дубликатов).
  - `app/main.py` — `include_router(proxies_router)`.
  - `app/templates/base.html` — пункт «Прокси» после «Аналитика».
  - `tests/test_proxies.py` — 21 тест: parse (8), mask (2), фейловер (2), веб (9). 486/486 ✅.
  - Исправлены моки в `tests/test_publishers.py` (2 теста) и `tests/test_publishers_video.py` (1 тест): `httpx.post` → `app.services.proxies.httpx.Client`.
  - httpx версия: 0.27.2; параметр прокси: `Client(proxy=url)`.
  - socks5 парсится и сохраняется, но check_proxy для socks5 возвращает (False, подсказка) — без пакета httpx[socks].
- **Сделано (2026-06-11, фикс видимости/удаления):**
  - **Каскадное удаление проекта:** `web/projects.py` — 409-запрет заменён на каскад (собираем content_id → DELETE FROM projects → FK удаляют дочерние таблицы → файлы чистим). Функция `delete_content_files` вынесена в `app/services/cleanup.py` (единый источник). Кнопка в `detail.html` предупреждает о удалении ВСЕГО контента (onsubmit confirm). Тест `test_delete_project_with_content_forbidden` → `test_delete_project_with_content_cascades`.
  - **Видимость осиротевшего контента:** `_get_orphan_content` в `web/queue.py` — убран фильтр `NOT IN ('rejected')` (старый v1 контент был невидим), добавлено условие `OR p.status='archived'` (контент архивных проектов, даже с plan_item, видим). Добавлен роут `POST /queue/orphans/delete-all`. В шаблоне `queue/index.html` — кнопка «Удалить всё», русские статусы через `_orphan_status_labels`, пометка «(архив)» у проекта.
  - **_STATUS_LABELS** в `queue.py` дополнен статусами старого флоу: draft/text_review/production/review/approved/rejected/published/manual_done.
  - **Аудит модалки** `/queue/_modal.html`: кнопка «Перегенерировать» уже была на `error` и `generated` — ок. Роут `queue_item_regen` корректно обрабатывает `content_id=None` (ошибка без контента) — тупиков нет.
  - **Аудит /plan**: шахматка показывает ВСЕ статусы plan_items без фильтра — все 6 статусов (proposed/approved/rejected/generating/generated/error) видны. /plan + /queue покрывают все состояния pipeline.
  - **Новые тесты** `tests/test_visibility.py`: 10 тестов — осиротевший контент всех статусов, rejected видим, plan_items всех статусов visible, архивированный проект, delete-all + файлы. **pytest 463/465 (2 чужих падения proxies.py)**.
- **Сделано (2026-06-11, этап 7.5 — UX-редизайн по жалобам приёмки, оркестратор + 4 sonnet ∥):**
  - **БАГ «профиль не в тему» ПОЧИНЕН (оркестратор):** form.html слал поля `step1_name/step1_description/step1_goals`, а POST /projects/generate-profile ждал `name/description/goals` → LLM получал ПУСТЫЕ данные и сочинял generic-профиль «про маркетинг». Поля переименованы; весь шаг 1 обёрнут в #full-form-container (фрагмент заменяет его целиком — дубли полей исчезли); ветка ошибки фрагмента рендерит редактируемые поля шага 1. Вторая мина: в `.env` был продублирован блок плейсхолдеров из .env.example (python-dotenv берёт ПОСЛЕДНЕЕ значение → LLM_ROUTER_KEY затирался на your_virtual_key_here) — дубль удалён.
  - **Навигация/base.html (agent):** вкладки Главная · Проекты · Контент-план (/plan, бейдж proposed) · Публикация (/queue, бейдж manual_pending) · Аналитика; часы сервера с тикающими секундами в шапке (единая временная база платформы); CSS-контракт шахматок: `.board*`, чипы `.chip-{proposed|approved|rejected|generating|generated|planned|published|manual|error}`, точки `.dot-*`, `.legend`.
  - **Старый флоу УДАЛЁН:** web/review.py, web/calendar.py, web/manual.py, web/generate.py + шаблоны review/, calendar/, manual/, projects/_ideas_list.html; /review, /calendar, /manual → 404. Pipeline-модули (ideas, script, video_script) ОСТАВЛЕНЫ — используются новым флоу внутренне. Старые веб-тесты удалены (часть дочистил оркестратор скриптом после session limit агентов).
  - **Дашборд:** счётчики-ссылки нового флоу (в работе / ждут одобрения / готово / опубликовано 7д / ручная / ошибки) + баннер «⚠ Автопубликация выключена» при ENABLE_SCHEDULER=0.
  - **/queue «Публикация» (agent):** шахматка как в /plan (точки статусов, легенда, сегодня подсвечен), модалка с превью и действиями «Перегенерировать» (content+файлы+schedule удаляются, item→approved), «Удалить» (→rejected), «Отменить публикацию»; секция ручной публикации (копировать текст / скачать mp4 / картинка story / «Опубликовано ✓» через mark_manual_done); секция «Вне плана» — осиротевший контент с удалением (ответ на «контент улетает и хрен найдёшь»).
  - **ФИКС временной базы (agent):** process_due сравнивал planned_at (локальное намерение из плана) с UTC `datetime('now')` SQLite → публикации опаздывали на 3 часа (MSK). Теперь `_now_local()` = datetime.now(); решение проекта: ЕДИНАЯ база — локальное время сервера, его и показывают часы в шапке. (Вторая причина «не отправлялось»: ENABLE_SCHEDULER=0 в dev — теперь виден баннер.)
  - **/plan «Контент-план» (agent):** тумблер «⚡ Генерация одобренного: ВКЛ/ВЫКЛ» с пояснением (вместо «Генерация выкл»), русские подписи статусов чипами, легенда, «Удалить пункт» (только без контента, иначе 422 с подсказкой), «Одобрить все предложенные за месяц (N)», empty-state с инструкцией.
  - **Страница проекта (agent):** секция «Идеи» удалена; структура «1. Профиль (details) → 2. Площадки → 3. Запуск завода»; stage-чип; после запуска кнопки «→ Контент-план» и «→ Публикация»; подписи панели запуска выровнены, у числовых полей пояснения.
  - **Итог: pytest 459/459 ✅; смоук: 6 страниц → 200, /review /calendar /manual → 404, часы/вкладки/баннер на месте ✅; data/ чистая.** — полный цикл: проект из 3 полей + ИИ-профиль → стратегия per-платформа → rolling контент-планы → шахматка согласования /plan → фабрика одобренного (lookahead) → шахматка готового /queue → автопубликация по плану → аналитика → пересмотр стратегии. Спецификация и контракты — `PLAN.md → Этап 7`. Осталось: деплой этапа 7 на mako-play + прод-наладка (см. «Следующий шаг»).
- **Сделано (2026-06-10, этап 7, подготовка оркестратора):** спецификация этапа 7 с контрактами записана в PLAN.md; созданы роутеры-заглушки `app/web/launch.py`, `strategy.py`, `plan.py`, `queue.py` и подключены в main.py (агенты main.py НЕ трогают).
- **Сделано (2026-06-10, этап 7, ВОЛНА A — фундамент, 1 sonnet):** `app/catalog.py` (типы контента per платформа + хелперы), `db.py` (+strategies, +plan_items, миграции: projects.stage/settings, project_platforms.content_types, пересборка content под типы story/article, DEFAULT_PROJECT_SETTINGS + get_project_settings), `llm.py` (+6 FAKE-purposes: profile/strategy/strategy_revise/plan/item_post/item_story). 247/247 ✅
- **Сделано (2026-06-10, этап 7, ВОЛНА B — 2 sonnet ∥ 1 opus):** (1) двухшаговое создание проекта: prompts/profile.txt + pipeline/profile.py + POST /projects/generate-profile, полная форма вынесена в projects/_form_full.html; (2) панель запуска: web/launch.py (GET launch-panel lazy-load HTMX из detail.html, launch-settings с clamp горизонтов, launch → stage=running + strategies(generating), stop, toggle-pause plan/gen/autogen) + projects/_launch.html; (3) стратегия: pipeline/strategy.py build_strategy(strategy_id) (inputs → записка purpose='strategy_analysis' → strategy/strategy_revise-ветка при user_comment → фильтрация по каталогу/включённым платформам → active, прочие archived; ошибки → status=error без исключений), prompts/strategy*.txt, web/strategy.py + templates/strategy/ (generating-автообновление every 5s, ручная правка, revise, retry). **310/310 ✅**
- **Сделано (2026-06-10, этап 7, ВОЛНА C — 2 sonnet ∥ 1 opus):** (1) pipeline/planner.py + prompts/plan.txt (generate_plan по активной стратегии: только включённые типы, валидация дат/типов, дедуп по дате+платформе+типу+title) + services/brain.py (process_brain: 1 generating-стратегия за тик ИЛИ rolling-догенерация плана по 1 платформе за тик; исключения не выходят наружу); (2) pipeline/from_plan.py (generate_for_item: text → purpose='item_post' → content type post/article со структурой texts под существующие паблишеры; story → purpose='item_story' + Pollinations-картинка → files.image_path; video → строка ideas + generate_video_script → сразу status='production') + services/factory.py (process_factory: дожим отрендеренных видео → schedule, выбор 1 одобренного пункта в lookahead-окне при autogen=1 и не gen_paused; schedule planned_at = дата+время плана, manual-типы → manual_pending; без регенерации); (3) web/plan.py + templates/plan/ — шахматка: платформа→тип (details open), дни месяца, переключение месяц/год, модалка с правкой брифа, одобрение поштучно/за месяц, toggle autogen, «🗓 План» в base.html. Оркестратор: джобы process_brain (2 мин) и process_factory (1 мин) в scheduler.py; смоук 8 страниц → 200, 7 джобов зарегистрированы; data/ чистая. **388/388 ✅**
- **Сделано (2026-06-10, волна 1):**
  - Структура проекта: `app/`, `tests/`, `data/`, `requirements.txt`, `.env.example`, `.gitignore` обновлён
  - `app/config.py`, `app/db.py`, `app/security.py`, `app/llm.py` — базовый стек
  - `app/web/dashboard.py`, `app/web/projects.py` — CRUD проектов и площадок
  - Шаблоны Jinja2+HTMX, статика, промпты/.gitkeep
- **Сделано (2026-06-10, волна 2 — публикация, параллельный агент):**
  - `app/publishers/`, `app/services/scheduling.py`, `app/scheduler.py`, `app/main.py` (lifespan)
  - `tests/test_publishers.py` → 7 passed ✅
- **Сделано (2026-06-10, волна 2 — pipeline/генерация, этот агент):**
  - `app/prompts/ideas.txt` — русскоязычный промпт с ролью, профилем проекта, learnings, защитой от повторов, few-shot JSON-примером
  - `app/prompts/script.txt` — промпт сценария с адаптацией по площадкам, хуком, телом, CTA; спецификации 5 платформ
  - `app/pipeline/prompts.py` — загрузчик `load_prompt(name, **kwargs)`, безопасные плейсхолдеры <<VAR>>
  - `app/pipeline/ideas.py` — `generate_ideas(project_id, n)`: сбор контекста → LLM → устойчивый парсинг (```-фенсы, 1 retry) → БД
  - `app/pipeline/script.py` — `generate_script(idea_id)`: профиль + включённые площадки → LLM → валидация структуры (1 retry) → content(text_review) + idea(used)
  - `app/web/generate.py` — HTMX-роуты: генерация/ручное добавление идей, «Сделать пост», «Отклонить»; LLMError → человекочитаемые сообщения
  - `app/web/projects.py` — аддитивно: ideas + draft_count в project_detail
  - `app/templates/projects/_ideas_list.html` — HTMX-фрагмент: список идей, бейдж черновиков, кнопки
  - `app/templates/projects/detail.html` — аддитивно: секция «Идеи» с кнопкой генерации + indic. загрузки
  - `app/main.py` — аддитивно: `from app.web.generate import router as generate_router` + `include_router`
  - `tests/test_pipeline.py` — 16 тестов, все ✅
- **Сделано (2026-06-10, фикс тестов):**
  - `app/config.py` — `Settings` переписан: класс-атрибуты заменены на `__getattribute__`+`__setattr__` с двухуровневым lookup (_overrides → os.environ → default). Теперь и `monkeypatch.setenv`, и `monkeypatch.setattr` работают без замены синглтона.
  - `app/web/projects.py` — выделен `_build_detail_context(slug)`, единый хелпер для рендера detail.html; `project_delete` (409-путь) теперь использует хелпер — `ideas` и `draft_count` всегда передаются.
  - `app/templates/projects/_ideas_list.html` — `draft_count | default(0)` — защита от UndefinedError.
  - `tests/test_pipeline.py` — удалена обходная фикстура `ensure_fake_llm` (больше не нужна).
  - `tests/conftest.py` — убрано `cfg_module.settings = cfg_module.Settings()` (тоже не нужно).
- **Сделано (2026-06-10, волна 3 — дашборд ревью и планирования):**
  - `app/web/review.py` — очередь ревью (`/review`): список по всем проектам/фильтр; детальная страница с редактируемыми textarea+счётчиком символов; `approve` (→approved + опциональное планирование на несколько площадок); `reject` (→rejected + learning source='reject')
  - `app/web/calendar.py` — календарь (`/calendar`): вид месяц (сетка 7xN) и неделя; цвет по статусу; перенос/отмена/retry; боковая панель «Готово к планированию»
  - `app/web/manual.py` — ручная очередь (`/manual`): manual_pending → копировать текст (vanilla JS clipboard) + URL → mark_manual_done
  - `app/web/dashboard.py` — обновлён: 6 счётчиков (проекты, ревью, к планированию, 7 дней, ошибки, ручная очередь) + блок «Последние ошибки»
  - `app/templates_env.py` — единый Jinja2Templates с `get_nav_counts()` как глобалом (бейджи в навигации без дублирования)
  - `app/web/nav.py` — `get_nav_counts()`: ревью + manual_pending из БД
  - `app/templates/base.html` — навигация: +Ревью (бейдж), +Календарь, +Ручная очередь (бейдж); через `{% set _nav = get_nav_counts() %}`
  - Все модули web переведены на `from app.templates_env import templates` (единый экземпляр)
  - Шаблоны: `review/list.html`, `review/detail.html`, `calendar/month.html`, `calendar/week.html`, `calendar/entry.html`, `manual/list.html`
  - `tests/test_dashboard.py` — 23 теста покрывают все флоу
  - `app/main.py` — аддитивно: include_router(review/calendar/manual)
- **Сделано (2026-06-10, волна 4 — e2e-тесты + DEPLOY.md завода):**
  - `tests/test_e2e.py` — 3 сценария (A/B/C), полный прогон `pytest tests -q` → **64 passed** ✅ (61 старых + 3 e2e)
  - Сценарий A: создание проекта → площадки с Fernet-шифрованием → генерация идей → пост → ревью + правка → approve + планирование в прошлом → process_due → telegram published (dry-run, outbox-файл) + dzen manual_pending → manual_done с URL → dashboard + calendar.
  - Сценарий B: reject с причиной → learning(source='reject') в БД → patch ideas_module.chat → learning попадает в промпт следующей генерации.
  - Сценарий C: PUBLISH_DRY_RUN=0 + monkeypatch httpx → 3 тика attempts 0→1→2→3, статусы planned/planned/planned/error, retry-роут сбрасывает в planned.
  - `DEPLOY.md` — копипаст-инструкция деплоя завода в том же стиле, что LLMRouter/DEPLOY.md: /opt/kontentzavod, порт 8200, user kontentzavod, MemoryMax=768M (комментарий про 1.5GB на этапе 3 ffmpeg), basic_auth в Caddy (предупреждение + caddy hash-password), FERNET_KEY предупреждение при смене, PUBLISH_DRY_RUN=1 до токенов, ENABLE_SCHEDULER=1 на проде, FAKE_LLM=0.
- **Сделано (2026-06-10, волна 5 — видеоконвейер, оркестратор + 4 сабагента: 3 sonnet + 1 opus):**
  - Оркестратор заранее зафиксировал контракт: `config.py` (+FAKE_TTS, FAKE_ASSETS, PEXELS/PIXABAY_API_KEY, MEDIA_RETENTION_DAYS, FFMPEG/FFPROBE_BIN, тип 'int' в _FIELDS), `.env.example`, `requirements.txt` (+edge-tts==7.0.2, установлен в .venv)
  - `app/pipeline/tts.py` — synthesize_scenes(): per-сцена mp3 (edge-tts Dmitry/Svetlana), WordTiming/SceneAudio, тайминги в глобальной шкале, duration из ffprobe; FAKE_TTS=1 → тишина anullsrc + синтетические тайминги
  - `app/pipeline/subtitles.py` — build_ass(): 1080x1920, 2-3 слова на экран, karaoke {\k} подсветка текущего слова, MarginV=550
  - `app/pipeline/assets.py` — fetch_scene_assets(): футажи Pexels (portrait, height≥1280, не 4K) → фоллбэк Pixabay → фоллбэк картинка; слайдшоу Pollinations.ai (без ключа); pick_music(mood) из data/music/{mood}/; FAKE_ASSETS=1 → плейсхолдеры lavfi
  - `app/services/cleanup.py` — cleanup_after_render (удаляет media/{id}/assets/), rotate_media (ролики опубликованных старше MEDIA_RETENTION_DAYS + осиротевшие media-папки; files.purged=true); cron-джоб daily 04:00 UTC в scheduler.py
  - `app/pipeline/render.py` (opus) — render_video(): 5 проходов ffmpeg (сценные клипы → concat copy → голос AAC → финал с ducking sidechaincompress + вжигание .ass → превью jpg); mp4-футажи кроп/луп, jpg — Ken Burns zoompan с пред-апскейлом x2; хелпер escape_subtitles_path для Windows-путей
  - `app/prompts/video_script.txt` + `app/pipeline/video_script.py` — generate_video_script(idea_id, template): идея → JSON {video: {voice, mood, scenes[text, keywords(en)]}, посты-обвязка по площадкам (без dzen), features} → content(type=video_*, status='text_review'); заглушка purpose='video_script' в llm.py
  - `app/pipeline/produce.py` — produce_video(): tts → ass → assets → music → render (ленивый импорт _do_render) → cleanup → files(абс. пути) → status='review'; ошибки → files.produce_error + produce_attempts, статус остаётся production; process_production() — 1 контент/тик, attempts<3; джоб в scheduler.py (interval 1 мин, max_instances=1)
  - Веб: кнопки «🎬 Футажи»/«🎬 Слайдшоу» на идеях; ревью включает status='review'; видео в text_review — правка сцен + «В продакшн ▶»; в review — <video controls> + approve с планированием + «Перерендерить»; бейдж ошибки продакшна; `app/web/files.py` — GET /files/videos/{id}.mp4|.jpg
  - Тесты: test_tts.py (15), test_assets.py (16), test_render.py (9), test_video_flow.py (28)
- **Сделано (2026-06-10, волна 6 — публикация видео, оркестратор + 4 сабагента: 3 sonnet + 1 haiku):**
  - Оркестратор сам зафиксировал стыки ДО запуска агентов: `base.py` (+`PublishDeferred(retry_at)`, ветка youtube, files→vk), `scheduling.py` (process_due ловит PublishDeferred → перенос planned_at БЕЗ attempts+1)
  - `app/publishers/vk.py` — видео: video.save + wallpost=1 → multipart-аплоад → vk.com/video{owner}_{id}; клипы (shortVideo.create) недоступны обычным токенам — не делаем; без video_path — прежний wall.post
  - `app/publishers/telegram.py` — sendVideo (multipart, supports_streaming) при files.video_path, приоритет над sendPhoto
  - `app/publishers/youtube.py` (новый) — oauth refresh → resumable init → PUT, чистый httpx; URL youtube.com/shorts/{id}; title автодополняется "#Shorts"; квота YOUTUBE_DAILY_LIMIT=6 (config+.env.example): счётчик published за сегодня → PublishDeferred до полуночи UTC+5мин; 403 quotaExceeded → +12ч; privacy default private (до audit-верификации)
  - `app/web/manual.py` + `templates/manual/list.html` — ручная очередь: для IG текст из texts.instagram.caption (фоллбэк text); видео → плеер `<video>` с постером + «Скачать mp4» через /files/videos/{content_id}.mp4
  - Тесты: test_publishers_video.py (8), test_youtube.py (8), test_manual_video.py (3), e2e сценарий D в test_e2e.py (2: полный видео-цикл на 4 площадки + квота YT)
- **Сделано (2026-06-10, волна 7 — аналитика и самообучение, оркестратор + 4 сабагента: 3 sonnet + 1 opus):**
  - Оркестратор зафиксировал контракт сам: UNIQUE-индекс metrics(schedule_id, date) в db.py (идемпотентный INSERT OR REPLACE); джобы в scheduler.py добавил сам ПОСЛЕ агентов (collect_metrics daily 03:00 UTC, analyze_all weekly пн 05:00 UTC) — агентам scheduler.py был запрещён
  - `app/analytics/collector.py` — collect_metrics(): published за 30 дней (vk/youtube) → VK wall.getById / video.get, YouTube videos.list+oauth refresh → metrics; нет credentials → skip; ошибка одной публикации не валит сбор; dry-run URL пропускаются; Telethon для TG отложен (докстринг)
  - `app/web/analytics.py` + `templates/analytics/index.html` — /analytics: карточки, Chart.js (line по дням с datasets по платформам, bar средних), топ-5/анти-топ-5 с бейджами features, фильтры project/days, ручной ввод метрик (INSERT OR REPLACE, предзаполнение); пункт «Аналитика» в base.html; include_router в main.py
  - `app/analytics/analyzer.py` (opus) + `app/prompts/analyze.txt` — analyze_project/analyze_all: последний замер на публикацию + features → LLM (purpose='analyze', json_mode, 1 retry) → learnings source='analyzer' (weight clamp 0.5–2.0, дедуп по тексту, ≤5 за прогон, ≤10 активных/проект — лишние с меньшим weight деактивируются), deactivate только analyzer-инсайтов; <5 публикаций → skip без LLM; заглушка FAKE_LLM purpose='analyze'
  - A/B хуков: `app/prompts/script_ab.txt` + `script.py::generate_script_ab(idea_id)→(id_a,id_b)` — один вызов LLM → variants A/B (полные тексты площадок, разные hook_type) → 2 content c features.ab_variant + суффиксы [A]/[B] в title; кнопка «A/B пост» в _ideas_list.html + роут в generate.py; заглушка purpose='script_ab'
  - Тесты: test_collector.py (5), test_analytics.py (29), test_analyzer.py (7), test_ab.py (15)
- **Сделано (2026-06-10, прод-фикс + конкретизация деплоя):**
  - Прод-инцидент на mako-play разобран: завод получал 404 от роутера при генерации идей. Причина — systemd `EnvironmentFile=` НЕ удаляет inline-комментарии: `LLM_ROUTER_BASE_URL=https://router.mak-o.ru/v1/ # комментарий` уезжал в openai SDK целиком с хвостом. Прямой питон-тест работал, потому что без systemd значение берёт python-dotenv (комментарии режет).
  - `app/config.py` — харденинг: `_SINGLE_TOKEN_FIELDS` (URL/ключи — первый токен значения), bool/int тоже отсекают хвост; `FFMPEG_BIN/FFPROBE_BIN` не трогаем (пути с пробелами). `tests/test_config_sanitize.py` — 6 тестов.
  - `DEPLOY.md` (завод) и `LLMRouter\DEPLOY.md` переписаны сабагентом под конкретные значения боевого сервера (mako-play, /srv/KontentZavod, /srv/LLMRouter, router.mak-o.ru, порт 8200) — раздел сессионных переменных удалён, команды копипаст-готовы. Юнит завода — БЕЗ EnvironmentFile (приложение само читает .env), MemoryMax=1536M, .env-шаблон дополнен всеми переменными, новая типовая проблема «404 от роутера» в диагностике. Единственный плейсхолдер — ДОМЕН_ЗАВОДА (домен пользователя в Caddy нам неизвестен).
- **Сделано (2026-06-10, этап 7 волна A — фундамент «Завод 2.0»):**
  - `app/catalog.py` (новый) — каталог типов контента per платформа: 5 платформ × их типы (telegram: post/video, vk: post/video/story, instagram: post/story/reel, youtube: short, dzen: post/article); хелперы `allowed_types`, `default_types`, `type_info`, `is_valid`; докстринги на русском.
  - `app/db.py` — добавлен `import json`; добавлен `DEFAULT_PROJECT_SETTINGS` (plan_horizon_days=30, gen_lookahead_days=3, retention_days=14, plan_paused=0, gen_paused=0, autogen=0); новые таблицы `strategies` и `plan_items` в SCHEMA; функция `_migrate(conn)` (4 идемпотентные операции: projects.stage, projects.settings, project_platforms.content_types, пересборка content с расширенным CHECK); `init_db()` теперь вызывает `_migrate`; хелпер `get_project_settings(row_settings_json) → dict`.
  - `app/llm.py` — добавлены 7 FAKE-заглушек: `_FAKE_PROFILE`, `_FAKE_STRATEGY`, `_FAKE_STRATEGY_REVISE`, `_FAKE_PLAN`, `_FAKE_ITEM_POST`, `_FAKE_ITEM_STORY`; `chat()` обработывает purposes profile/strategy/strategy_revise/plan/item_post/item_story; докстринг обновлён.
  - `tests/test_foundation_v2.py` (новый) — 44 теста: каталог (18), миграции (16), пересборка content на старой БД (1), FAKE-заглушки LLM (9).
- **Сделано (2026-06-10, этап 7 волна B — панель запуска, этот агент):**
  - `app/web/launch.py` — наполнен: 5 роутов (launch-panel, launch-settings, launch, stop, toggle-pause/{plan|gen|autogen}); хелперы `_get_enabled_platforms`, `_get_strategy_status`, `_clamp`, `_save_settings`, `_build_launch_context`; 404 для неизвестного slug; валидация «нет платформы с включённым типом» → 422.
  - `app/templates/projects/_launch.html` — фрагмент «🚀 Запуск завода»: статусная строка (stage + статус стратегии + ссылка «📋 Стратегия»), форма с чекбоксами типов по платформам (хелпер каталога, пометка «вручную» для manual-типов), числовые поля (plan_horizon_days, gen_lookahead_days, retention_days), кнопки «Сохранить» / «▶ В работу» / «⏹ Остановить»; тумблеры пауз plan/gen + бейдж autogen (только при stage='running').
  - `app/templates/projects/detail.html` — одна строка lazy-load `<div id="launch-panel" hx-get=... hx-trigger="load">` после секции площадок.
  - `tests/test_launch.py` — 39 тестов: 9 (launch-panel), 7 (launch-settings), 8 (launch), 3 (stop), 11 (toggle-pause), 2 (интеграционный флоу).
- **Результат (этап 7 волна A):** `pytest tests -q` → **247 passed** (203 старых + 44 новых) ✅
- **Результат (прод-фикс):** `pytest tests -q` → **203 passed** ✅
- **Результат (волна 7):** `pytest tests -q` → **197 passed** ✅; смоук оркестратора: 7 URL (включая /analytics) → 200, при ENABLE_SCHEDULER=1 зарегистрированы все 5 джобов (analyze_all, collect_metrics, process_due, process_production, rotate_media) ✅; продовая data/ чистая ✅
- **Результат (волна 6):** `pytest tests -q` → **153 passed** ✅; смоук оркестратора `/` `/review` `/calendar` `/manual` `/projects` → 200, `/files/videos/999.mp4` → 404 ✅; продовая data/ чистая ✅
- **Результат (волна 5):** `pytest tests -q` → **132 passed** ✅; сквозная интеграция оркестратора (без моков рендера, настоящий ffmpeg): оба шаблона → валидный 1080x1920 mp4 22.4с, assets подчищены, статус review ✅; смоук `/` `/review` `/calendar` `/manual` `/projects` → 200, `/files/videos/999.mp4` → 404 ✅; продовая data/ чистая
- **Запуск завода:** из `A:\DevAI\Projects\KontentZavod`: `.venv\Scripts\uvicorn app.main:app --reload`
- **Запуск роутера:** из `A:\DevAI\Projects\LLMRouter`: `.venv\Scripts\python -m uvicorn app.main:app --port 8100`
- **Сделано (2026-06-10, этап 7, ВОЛНА C — плanner + brain, sonnet):** `app/prompts/plan.txt` (промпт контент-директора: <<PROFILE>>/<<PLATFORM>>/<<PLATFORM_STRATEGY>>/<<CONTENT_TYPES>>/<<DATE_FROM>>/<<DATE_TO>>/<<LEARNINGS>>/<<USED_TOPICS>>); `app/pipeline/planner.py` (generate_plan: активная стратегия → секция платформы → включённые типы → learnings + used_topics → chat(purpose='plan') с 1 retry → валидация дата/тип/title → дедуп по (project_id,platform,content_type,date,title) → INSERT plan_items; LLMError пробрасывается); `app/services/brain.py` (process_brain: 1. generating-стратегии → build_strategy + ранний выход; 2. running-проекты → rolling-покрытие по платформам → generate_plan + ранний выход; всё в try/except). Тесты: `tests/test_planner.py` — 22 теста. **332/332 ✅**
- **Сделано (2026-06-10, этап 7, ВОЛНА D ч.1–2 — 2 sonnet ∥):** оркестратор до волны добавил GET /files/images/{content_id}.jpg в web/files.py (prefix роутера сменён на /files, URL видео не изменились). (1) web/queue.py + templates/queue/ — шахматка готового: GET /queue (plan_items generating/generated/error, индикаторы статуса публикации из schedule), модалка с превью post/video/story и скачиванием, POST cancel (DELETE schedule planned, иначе 422), POST retry (error→approved, сброс content_id), «📦 Очередь» в base.html; tests/test_queue.py 37 тестов. (2) web/projects.py — именованные формы credentials per-платформа (tg: bot_token+chat_id; vk: access_token+group_id; yt: client_id+client_secret+refresh_token; ig/dzen — инфо-блок) + POST /projects/{slug}/platforms/{platform}/check (httpx timeout=10: getMe / groups.getById / oauth refresh); cleanup.rotate_media — retention_days из настроек проекта (глобальный — фоллбэк); story в /manual (img + caption + скачать); /analytics — фильтр и таблица средних по content.type; tests/test_naladka.py 34 теста + 3 старых теста переведены на именованные поля. Оркестратор проверил сам: **pytest 459/459 ✅, смоук 9 страниц → 200 (вкл. /queue), /files 404 корректны, 7 джобов, data/ чистая (БД пустая)**.
- **Сделано (2026-06-10, этап 7, ВОЛНА D ч.3 — 1 sonnet):** tests/test_e2e.py — сценарий E (TestScenarioE): generate-profile → проект → telegram именованными полями (Fernet проверен) → launch-settings + launch → process_brain (тик 1: стратегия active, тик 2: plan_items) → /plan approve + autogen → process_factory (content + schedule planned) → planned_at в прошлое → process_due → published dry-run://, outbox-файл → /queue 🚀 + модалка → паузы plan_paused/gen_paused реально стопорят brain/factory. DEPLOY.md дополнен: миграции этапа 7 идемпотентны (применяются при рестарте), джобы process_brain/process_factory, абзац «Воркфлоу 2.0» (старый флоу работает параллельно). Оркестратор подтвердил: **pytest 460/460 ✅, data/ чистая**. Багов в app/ сценарий E не вскрыл. **ЭТАП 7 РЕАЛИЗОВАН.**
- **Следующий шаг (выполняет ПОЛЬЗОВАТЕЛЬ):** Деплой 7.8–7.11 на mako-play: `git pull` → restart (новых зависимостей НЕТ). Убедиться что PEXELS_API_KEY и PIXABAY_API_KEY заданы в .env. После деплоя: открыть настройки проекта → секция «🎬 Озвучка и субтитры» → проверить предпросмотр субтитров и кнопку «▶ Прослушать». YouTube OAuth + audit — позже.

## Что нужно от пользователя (блокеры)

- ~~DeepSeek-ключ~~ ✅ лежит в `.env` (DEEPSEEK_API_KEY) и добавлен в роутер. Пользователь обещал перевыпустить ключ (он засветился в чате) — при 401 спросить новый.
- Free-ключи (Gemini aistudio.google.com / Groq console.groq.com / OpenRouter) — пользователь может добавлять сам через кабинет http://127.0.0.1:8100/admin (пароль changeme — сменить в .env роутера!).
- Данные своих проектов пользователь введёт САМ через дашборд `/projects` (профиль: описание, ЦА, тон, цели, CTA, темы).
- Деплой роутера: пользователь выполняет сам по `LLMRouter\DEPLOY.md` (после деплоя: сменить ADMIN_PASSWORD, перевыпустить виртуальные ключи).
- Токены TG-бота / VK-сообщества — ОТЛОЖЕНО по решению пользователя (подтверждено 2026-06-10, волна 6: «ключи не дам, нет времени»): всё проверяем e2e без реальных API (FAKE_LLM + dry-run паблишеры), реальные токены — после полной реализации.
- YouTube: пользователю нужно будет создать OAuth-приложение в Google Cloud Console (client_id/client_secret/refresh_token в credentials площадки) и подать заявку на audit-верификацию (до одобрения видео лочатся в private — поэтому default privacy='private'). VK-токен сообщества должен иметь права video+wall.

## Нюансы и грабли (накапливаем)

- **.env-мина (РЕШЕНА, 2026-06-11):** в локальный .env был дописан блок плейсхолдеров из .env.example. python-dotenv при дублях ключей берёт ПОСЛЕДНЕЕ значение → реальные ключи молча затирались. Правило: в .env никаких дублей, .env.example не копировать в конец.
- **Временная база (РЕШЕНИЕ, 2026-06-11):** везде ЛОКАЛЬНОЕ время сервера. planned_at = «дата + time_slot» плана (локальное намерение), сравнение в process_due — `_now_local()` (datetime.now()), часы в шапке показывают то же. SQLite `datetime('now')` — это UTC, для сравнений с planned_at НЕ использовать.
- **HTMX-формы: имена полей.** hx-include включает инпуты с их атрибутом name — он обязан совпадать с параметрами Form(...) роута (баг «профиль не в тему»: step1_name vs name → LLM получал пустые строки и галлюцинировал generic-контент). При любой HTMX-форме сверять name ↔ Form.
- **HTMX: исчезающий hx-target (грабли 7.7).** Если кнопка делает `hx-swap="outerHTML"` по `hx-target="#X"`, а возвращаемый фрагмент НЕ содержит элемент с id="X" — первый клик срабатывает, все последующие HTMX молча игнорирует (target не найден). Симптом «кнопка работает раз в 10 нажатий / только после F5» (панель запуска: #launch-panel vs #launch-panel-inner). Правило: id корня фрагмента == hx-target всех его кнопок; для перерисовываемых зон держать устойчивую обёртку (#plan-board-area).
- **Jinja-глобалы-функции вызывать со скобками:** `now_epoch` зарегистрирован как `time.time` (функция) — `{{ now_epoch }}` рендерит repr → parseFloat=NaN (часы «NaN:NaN:NaN»). Только `{{ now_epoch() }}`.
- **VK API ≥5.141:** `groups.getById` возвращает `{"response": {"groups": [...]}}`, НЕ плоский список. `groups[0]` на dict даёт `KeyError: 0` → в UI было «✗ Ошибка: 0». Обрабатывать оба формата.
- **contextmanager БД: `__enter__` ровно один раз.** Повторный `__enter__()` у `_GeneratorContextManager` → `AttributeError: no attribute 'args'` (валило публикацию TG при настроенных прокси). В request_via_proxy соединение открывается один раз в `conn`.
- **edge-tts 7.2.x:** дефолтный boundary сменился на SentenceBoundary — для пословных karaoke-таймингов ОБЯЗАТЕЛЬНО `Communicate(..., boundary="WordBoundary")`. 403 от speech.platform.bing.com лечится апгрейдом (7.2.8) + фейловером через прокси-пул (параметр `proxy=` у Communicate).
- **Разметка публикаций:** LLM может вернуть markdown несмотря на запрет в промпте — конвертация на этапе публикации обязательна: TG → `md_to_telegram_html` (parse_mode=HTML), VK → `md_to_plain`; IG/Дзен конвертируются при генерации (from_plan). Новые паблишеры — только через `app/services/textfmt.py`.
- **Текущий проект (7.7):** cookie `current_project` (slug) ставится при открытии /projects/{slug}; `app/web/current.py::get_current_project(request)` — единственный источник; /plan и /queue фильтруются по нему, без cookie показывают всё.
- **CSS-контракт UI (2026-06-11):** шахматки и статусы во всех вкладках используют классы из base.html: `.board*`, `.chip-<status>`, `.dot-<status>`, `.legend`. Новые страницы — только через них, не плодить локальные стили статусов.
- **ASS karaoke-семантика цветов (7.11):** в libass при karaoke `{\k}` PrimaryColour — цвет УЖЕ произнесённого/подсвеченного слова, SecondaryColour — ещё НЕ произнесённого. Поэтому: PrimaryColour=highlight_color (жёлтый), SecondaryColour=font_color (белый). Это контринтуитивно, но именно так работает libass. Format-строка стилей обязана содержать PrimaryColour/SecondaryColour/OutlineColour/BackColour — без этих полей libass берёт дефолты (чёрный текст).
- **TTS preview кеш (7.11):** GET /tts/preview/{voice} кеширует mp3 в data/tts_preview/{voice}.mp3. При смене ffmpeg или edge-tts нужно вручную удалить кеш-файлы для пересинтеза. FAKE_TTS=1 кешируется тоже (тишина) — при переходе с FAKE на реальный TTS удалить data/tts_preview/*.mp3.
- **mock fake_build_ass в тестах:** при добавлении новых kwargs в build_ass все `fake_build_ass(words, out_path, words_per_line=3)` в тестах нужно дополнять `**kwargs` — иначе TypeError при вызове produce_video.

- **HTTP-прокси (2026-06-11):** `app/services/proxies.py` — главная функция `request_via_proxy(method, url, _db=conn, **kwargs)`. httpx версия 0.27.2, параметр `Client(proxy=url)` (не `proxies=`). socks5 парсится и сохраняется в БД, но `check_proxy` возвращает (False, подсказка) — без пакета httpx[socks]. `list_active_proxies` graceful при отсутствии таблицы (OperationalError → []). Telegram-тесты с моком `httpx.post` переведены на `app.services.proxies.httpx.Client`. ВСЕ новые вызовы api.telegram.org — только через `request_via_proxy` (сервер в РФ).
- **Pollinations МЁРТВ для анонимов (2026-06-11):** image.pollinations.ai отдаёт 402 со ВСЕХ IP (проверено 10 прокси) — анонимный тир закрыт, это не блокировка по IP. Картинки — только Pexels/Pixabay Photo API (`assets.py::fetch_image`); Pollinations в цепочке остаётся, но вызывается ТОЛЬКО при заданном POLLINATIONS_TOKEN. **Дополнение (вторая приёмка): 402 приходит и С токеном — бесплатного тира нет вовсе, источник платный.**
- **Прокси, отвечающий 402 на всё, — исчерпанный тариф (2026-06-11):** webshare-подобные прокси при исчерпании трафика возвращают `402 Payment Required` на ЛЮБОЙ запрос. Не путать с 402 целевого сайта. Признак: все прокси пула дают одинаковый 402 на разные домены. Такие прокси надо авто-дизейблить, а не перебирать по кругу (10 прокси × каждый запрос = минуты впустую на каждый ассет).
- **Pexels блокирует дата-центр IP (2026-06-11):** с серверного IP `/v1/search` (фото) → 404, CDN `videos.pexels.com` → 403; с резидентного IP всё 200 (даже без API-ключа). Код запроса корректен — менять ключ/заголовки бесполезно, нужен другой источник или резидентный маршрут.
- **VK video.save требует ПОЛЬЗОВАТЕЛЬСКИЙ токен (2026-06-11):** ошибка 27 «method is unavailable with group auth» — групповой токен не умеет video.save (ограничение VK API, кодом не обходится). Нужен user token (scope video+wall+offline, например через vkhost), загрузка в группу — параметр group_id у video.save. wall.post (текст/фото) групповым токеном работает.
- **Секреты в query-string попадают в логи:** Pollinations-токен светится полным URL в journalctl (`token=sk_…`). URL с токенами перед логированием маскировать (как mask_proxy_url).
- **ASS karaoke цвета (2026-06-11):** в karaoke-{\k} PrimaryColour — цвет УЖЕ подсвеченного текста, SecondaryColour — ещё не произнесённого (контринтуитивно). Поэтому в build_ass: Primary=highlight_color, Secondary=font_color. Format-строка стилей ОБЯЗАНА содержать поля цветов — без них libass берёт свои дефолты (баг «чёрные субтитры»).
- **Кеш tts-preview:** `data/tts_preview/{voice}.mp3` не сбрасывается автоматически — после переключения FAKE_TTS → реальный TTS удалить файлы кеша.
- **Прокси-фейловер assets.py (2026-06-11):** _http_get и _download_stream — прямая попытка (1 retry) → при 402/403/429 или TransportError/TimeoutException → перебор HTTP-прокси через `_get_http_proxies()` (аналог tts.py). Для stream-запросов через прокси: `httpx.Client(proxy=url)` + `client.stream(...)`. Мокать в тестах через `app.pipeline.assets.httpx` (не глобальный httpx). Pollinations: 1-я попытка с nologo=true → при 402 повтор без nologo. Токен POLLINATIONS_TOKEN (пустая строка по умолчанию) добавляется в URL если задан.
- **Кириллица в curl из git-bash (2026-06-11):** Bash-tool на Windows передаёт кириллицу аргументов curl в cp1251 → на сервер уходит мусор (симптом на приёмке: LLM получил кракозябры и сгенерил профиль «про чат-боты» — выглядело как баг завода, оказалось артефактом теста). Прод с кириллицей тестировать ТОЛЬКО скриптом через `.venv\Scripts\python` + httpx (utf-8 гарантирован).
- **Сабагенты: уровень размышления (2026-06-11):** поле `effort: low|medium|high|xhigh|max` в frontmatter `.claude/agents/*.md` — переопределяет уровень сессии. Всем трём агентам выставлен `medium` (решение пользователя — не жечь токены на максимальном размышлении).

- **Каскадное удаление (2026-06-11):** `PRAGMA foreign_keys=ON` в `get_db` уже включён — DELETE FROM projects удаляет все дочерние таблицы (project_platforms, ideas, content → schedule → metrics, learnings, strategies, plan_items). Файлы content удаляются из `app/services/cleanup.py::delete_content_files` ПОСЛЕ закрытия соединения. Эта функция — единственный источник логики удаления файлов (queue.py и projects.py оба импортируют её).
- **Видимость контента v1 (2026-06-11):** контент старого флоу без plan_item теперь всегда виден в /queue «Вне плана» (все статусы, включая rejected). Контент архивированных проектов — тоже, даже если есть plan_item (шахматки /plan и /queue показывают только активные проекты). Роут `POST /queue/orphans/delete-all` удаляет всю выборку за один клик.
- **Лимит подписки и сабагенты (2026-06-10, этап 7):** при ударе в session limit сабагенты возвращают «You've hit your session limit» вместо отчёта, НО успевают дописать файлы. Не верить пустому отчёту — проверять фактическое состояние (Glob файлов + полный pytest): в волне C оба «обрезанных» агента (factory, шахматка) на деле всё доделали, 388/388.
- **Этап 7, стыки:** заглушки web-роутеров создаёт оркестратор и подключает в main.py ДО запуска волн — параллельные агенты main.py/scheduler.py не трогают. Панель запуска грузится в detail.html через HTMX lazy-load (`hx-get="/projects/{slug}/launch-panel" hx-trigger="load"`) — projects.py и launch.py не конфликтуют.

- **systemd-ловушка .env (РЕШЕНА, 2026-06-10):** `EnvironmentFile=` в юните НЕ удаляет inline-комментарии («KEY=значение # пояснение» приходит целиком) и значения побеждают python-dotenv (load_dotenv не перезаписывает os.environ). Симптом: 404 от роутера при рабочем curl. Правила: в серверном .env комментарии ТОЛЬКО отдельной строкой; юнит завода — без EnvironmentFile; в config.py харденинг `_SINGLE_TOKEN_FIELDS`. Прод: mako-play, /srv/KontentZavod, root; после rsync под root обязателен chown -R kontentzavod.
- **Публикация видео (волна 6):** `PublishDeferred(retry_at)` из base.py — НЕ ошибка: process_due переносит planned_at и НЕ увеличивает attempts (для дневных квот). Квота YouTube считается по `schedule: platform='youtube' AND status='published' AND date(updated_at)=date('now')` — проверяется и в dry-run (чистая работа с БД). В тестах process_due патчить `app.services.scheduling.publish` (from-import при загрузке модуля), не `app.publishers.base.publish`. PS 5.1: `python -c "..."` через переменную теряет внутренние кавычки — однострочные питон-проверки писать во временный .py-файл.
- **Видеоконвейер (волна 5):** ffmpeg/ffprobe локально — `C:\FFmpeg\bin` (в PATH); на сервере — `apt install ffmpeg` (добавить в DEPLOY.md на этапе 4). `subtitles=`-фильтр на Windows требует экранирования пути (`C\:/...`) — есть хелпер `render.escape_subtitles_path`. concat demuxer с `-c copy` требует строго одинаковых параметров сценных клипов (один пресет + setsar=1). zoompan дрожит на маленьком входе — лечится пред-апскейлом x2. Karaoke ASS: пауза между словами добавляется к `\k` СЛЕДУЮЩЕГО слова.
- **Рендер сторис (8.5A):** наложение текста — через ASS/libass (НЕ drawtext): `story_render.render_story_slides` → ASS-плашка (BorderStyle=3, BackColour &H96000000) + `ffmpeg -loop 1 -i bg -vf "scale=...:crop=1080:1920,subtitles='<esc>'" -frames:v 1`. position→ASS alignment (top=8/center=5/bottom=2). Эмодзи из текста наложения ВЫРЕЗАЮТСЯ (`_strip_emoji`) — libass без emoji-шрифта даёт «тофу»; эмодзи живут только в caption поста. **ДЕПЛОЙ: на сервере нужен кириллический шрифт — `apt install fonts-dejavu`, затем `STORY_FONT=DejaVu Sans` в .env** (локально дефолт Arial). Без шрифта libass не отрендерит русский. В тестах рендер мокается через `app.pipeline.story_render._run_ffmpeg` + `fetch_image`; для from_plan-тестов мокать `app.pipeline.from_plan.render_story_slides`.
- TestClient БЕЗ контекст-менеджера не запускает lifespan (init_db) → «no such table». Смоук только через `with TestClient(app) as c:`.
- В тестах рендер мокается через `app.pipeline.produce._do_render` (ленивый импорт render_video внутри produce — НЕ импортировать на уровне модуля).
- Pixabay Videos API не умеет orientation — берём medium/small, рендер кадрирует. Pollinations.ai: GET image.pollinations.ai/prompt/{prompt}?width=1080&height=1920&nologo=true, без ключа, промпт на английском.
- **Pexels Photos API (2026-06-11):** `GET https://api.pexels.com/v1/search`, заголовок `Authorization: {key}`, параметры `query/orientation=portrait/per_page`. Ответ: `{"photos": [{"src": {"portrait": "...", "large2x": "..."}, ...}]}`. Предпочтение: `src.portrait` (вертикальный обрезанный), фоллбэк `src.large2x`.
- **Pixabay Photos API (2026-06-11):** `GET https://pixabay.com/api/`, параметры `key/q/orientation=vertical/image_type=photo/per_page`. Ответ: `{"hits": [{"largeImageURL": "...", "webformatURL": "..."}, ...]}`. Используем `largeImageURL` (полное разрешение). ВАЖНО: Pixabay Photos и Pixabay Videos — РАЗНЫЕ эндпоинты (`/api/` и `/api/videos/`), один и тот же ключ работает на обоих.
- **fetch_image цепочка (2026-06-11):** Pollinations вызывается ТОЛЬКО если POLLINATIONS_TOKEN непустой (без токена — 402 со всех IP). Мокировать в тестах через `app.pipeline.assets.fetch_image` (for from_plan.py) или `app.pipeline.assets._fetch_pexels_photo` / `_fetch_pixabay_photo` напрямую.

- **Схема работы:** оркестратор + сабагенты. Кастомные агенты из `.claude/agents` видны только сессиям, начатым ПОСЛЕ их создания; если тип не найден — `general-purpose` с параметром model (haiku/sonnet/opus).
- PowerShell 5.1 читает `.ps1` без BOM как ANSI → в скриптах хуков только ASCII. Execution policy = Restricted → хук запускается через `Get-Content ... | Invoke-Expression` (см. settings.json).
- Starlette 1.2.x: `TemplateResponse(request, name, ctx)` — request первым аргументом (в admin.py обёртка `_tmpl`).
- SQLite `SUM(CASE...)` на пустой таблице даёт None → COALESCE.
- DeepSeek в финальном SSE-чанке отдаёт `usage` — парсится для подсчёта токенов в stream-режиме.
- Кракозябры в выводе Invoke-RestMethod — особенность консоли PS 5.1, JSON роутера в норме (UTF-8).
- **Долг (мелочи, поправить при случае):** (1) в кабинете полный vk-ключ передаётся через redirect URL → попадает в access-логи uvicorn, лучше через одноразовую сессию; (2) manage.py дублирует часть логики services.py — свести к одному источнику.
- **FAKE_LLM-ловушка (РЕШЕНА):** `Settings` переписан на `__getattribute__`+`__setattr__` с двухуровневым lookup. Теперь `monkeypatch.setenv("FAKE_LLM", "1")` из `conftest.patch_env` работает во ВСЕХ тест-файлах без дополнительных хаков. `monkeypatch.setattr(settings, "PUBLISH_DRY_RUN", True)` тоже работает (сохраняется в `_overrides`). НЕ возвращать class-level атрибуты в config.py.
- Тесты до фикса успели намусорить в продовой `data/kontentzavod.db` и `data/outbox/` — оркестратор очистил (2026-06-10), БД пересоздаётся на старте. Полный pytest 38/38 подтверждён оркестратором.

- Сервер: макс 4GB RAM / 2 vCPU / 30GB SSD → никаких Postgres/Redis/Celery/n8n/moviepy. SQLite + APScheduler + чистый ffmpeg.
- У пользователя RX 580 (AMD) — локальная генерация невозможна, всё через API.
- Платный бюджет: только DeepSeek API. Остальное — free-тиры и бесплатные сервисы.
- Браузер-автоматизация: отложена, только этап 6, только Gemini, только если пользователь захочет.
- Контент на русском. Несколько проектов: динамические, в БД, CRUD через дашборд (НЕ projects.yaml). Per-project креденшелы площадок — Fernet в project_platforms.
- Публикация: TG/VK авто; YouTube авто с оговоркой (audit); IG/Дзен — ручная очередь через дашборд.

- **Shared templates (волна 3):** `app/templates_env.py` — единственный экземпляр `Jinja2Templates`. Все web-модули импортируют `from app.templates_env import templates`. Jinja2-глобал `get_nav_counts` вызывается прямо из `base.html` (`{% set _nav = get_nav_counts() %}`). При добавлении нового web-модуля: НЕ создавать локальный `templates`, импортировать из `templates_env`.
- Автопереключение модели между сессиями: поле `"model"` в `.claude/settings.json` проекта (обновляет Claude в конце сессии). Сейчас стоит `sonnet` под этап 1.
- Stop-хук контроля свежести STATE.md заблокирован классификатором разрешений — ждёт решения пользователя (скрипт описан в журнале сессии 2026-06-10).

## Журнал сессий

- **2026-06-13 (2)** — **ЭТАП 8.3 ВЫПОЛНЕН: слой «цензор»** (оркестратор спроектировал, coder-агент написал код). Двухуровневая защита: превентивный (<<RULES>> в промпты) + реактивный (LLM-вызов purpose='censor', ≤2 регенерации → CensorError → status=error). app/pipeline/censor.py (check_content, CensorError, fail-open при техсбое), app/prompts/censor.txt, FAKE_CENSOR в llm.py, цензор-петли в from_plan.py и video_script.py, load_rules() в prompts.py. Старые тесты не упали. **pytest 697/697 ✅** (+10 новых в tests/test_censor.py).

- **2026-06-13 (1)** — **ЭТАП 8.2 ЗАВЕРШЁН: волны D + E** (оркестратор Opus 4.8 — Fable 5 отключена; + 2 sonnet-агента). Стартовое состояние: код волны D (UI директив + goal-бейдж + парсинг рубрик с целью) был написан прошлой срезанной лимитом сессией и лежал в рабочей копии НЕзакоммиченным, но БЕЗ тестов (673/673 = как после C). Оркестратор: сверил код волны D с контрактом (всё на месте — эндпоинт strategy_directive_action done/dismissed→303 + 404/422, кнопки управления директивами, парсинг рубрик «Название｜goal｜описание» закрыл долг волны B, goal-бейдж модалки, check_warnings жёлтым из B, пояснения revise-формы) → ТЗ sonnet-агенту: +10 тестов (TestStrategyDirectives ×6, TestStrategyEditRubricsGoal ×2, модалка goal ×2). Затем волна E: оркестратор спроектировал детерминированный e2e (через generate_plan, не недетерминированные тики brain) + сам дописал DEPLOY.md (миграции 6/7 + слой совместимости легаси); sonnet-агент написал tests/test_e2e_directives.py (4 теста): директива per_week=7 перебивает mix 3→7 → ровно 7/нед на 7 разных дней с goal (жалоба «говорю 7, делает 3» закрыта через директиву), легаси-стратегия даёт план (content_mix=4 → 4 пункта), revise → фазовая v2 + директива. Итог: **pytest 687/687 ✅** (673 → +10 D → +4 E). Не закоммичено (коммит за пользователем). Дальше — деплой 8.1+8.2 на mako-play + приёмка ЛИБО этап 8.3 (цензор закона РФ/18+); спросить пользователя порядок.

- **2026-06-11 (3)** — **ЭТАП 7.7: фиксы второй приёмки + крупный UX** (оркестратор сам диагностировал ВСЕ баги по коду и логам и сам исправил точечные, затем 4 sonnet-агента: 3 ∥ + 1). Оркестратор: двойной `__enter__` в request_via_proxy (корень «_GeneratorContextManager…args» при публикации TG), `{{ now_epoch() }}` (часы NaN), новый формат groups.getById VK ≥5.141 («Ошибка: 0»), исчезающий hx-target панели запуска (#launch-panel → #launch-panel-inner, «кнопка срабатывает раз в 10 нажатий») + флеш «✓ Настройки сохранены». Агенты: (B) textfmt.py — markdown→HTML для TG / plain для VK+IG+Дзен + image_prompt и Pollinations-картинка к каждому посту + фото в TG (caption≤1024) и VK (saveWallPhoto); (C) edge-tts 7.2.8 + boundary=WordBoundary + фейловер синтеза через прокси-пул (фикс 403); (E) текущий проект: cookie + переключатель в шапке + вкладка «Проект» + per-project /plan и /queue + «← К списку проектов» + «✓ Сохранено» в модалке плана; (D) refresh_plan — перегенерация только proposed (вся/платформа/тип) с кнопками 🔄 на /plan. Итог: **pytest 573/573 ✅ (было 486), смоук оркестратора 7 страниц 200 + epoch число + cookie + refresh-UI ✅, data/ чистая**. Дальше — деплой 7.6+7.7 (с pip install -U!) + ENABLE_SCHEDULER=1 (пользователь).

- **2026-06-11 (2)** — **ЭТАП 7.6: жизненный цикл + каскадное удаление + прокси + приёмка прода** (оркестратор + 2 sonnet ∥, агентам выставлен effort: medium). Агент 1: каскадное удаление проектов (409 отменён), «Вне плана» показывает ВСЁ осиротевшее (включая rejected — корень жалобы «контент невидим и неудаляем») и контент архивных проектов, delete-all, тест «ничего не теряется». Агент 2: прокси-подсистема (таблица proxies + Fernet, parse_proxy_input понимает curl-формат, request_via_proxy с автофейловером, /proxies в админке, telegram.py через пул). Оркестратор: getMe через прокси, контрольный pytest **486/486 ✅**, смоук 7 страниц 200. Приёмка прода по HTTP (браузер-скилла в среде нет): ИИ-профиль В ТЕМУ (ложная тревога «про чат-боты» = cp1251-артефакт git-bash curl), цикл до стратегии работает, смоук 200; **найден блокер ENABLE_SCHEDULER=0 на проде**. Дальше — деплой 7.6 + ENABLE_SCHEDULER=1 + прокси на /proxies (пользователь).

- **2026-06-11 (1)** — **ЭТАП 7.5: UX-РЕДИЗАЙН ПО ПРИЁМКЕ** (жалобы пользователя после первого знакомства с 2.0). Оркестратор сам нашёл и починил корень «профиль не в тему» (несовпадение имён полей HTMX-формы → пустые данные в LLM + дубль плейсхолдеров в .env), затем 4 параллельных sonnet: (A) nav 2 вкладки + часы сервера + CSS-контракт чипов/шахматок + снос review/calendar/manual/generate + новый дашборд с баннером планировщика; (B) /queue «Публикация» — шахматка, ручная публикация на той же странице, перегенерация/удаление контента, «Вне плана», фикс UTC→локальное в process_due; (C) /plan «Контент-план» — понятные подписи («Генерация одобренного»), легенда, удаление пунктов; (D) страница проекта без «Идей», структура 1-2-3. Инцидент: 3 из 4 агентов снова срезаны session limit, но работу дописали (подтверждён нюанс от 2026-06-10) — оркестратор дочистил 20 старых тест-классов скриптом. Итог: **pytest 459/459 ✅, смоук 200/404 ✅, data/ чистая**. Дальше — деплой и повторная приёмка.

- **2026-06-10 (8)** — **ЭТАП 7 ЗАВЕРШЁН: ВОЛНА D** (оркестратор + 3 sonnet: /queue ∥ наладка, затем e2e). Оркестратор до волны сам добавил GET /files/images/{id}.jpg (web/files.py, prefix → /files) — развёл конфликт двух агентов. Агент 1: /queue шахматка готового (модалка-превью post/video/story, скачивание, cancel/retry, «📦 Очередь» в nav, 37 тестов). Агент 2: именованные формы credentials + POST /check (httpx-проверки TG/VK/YT), retention per-project в rotate_media, story в /manual, /analytics по типам (34 теста). Агент 3: e2e-сценарий E полного нового флоу + DEPLOY.md (миграции/джобы/«Воркфлоу 2.0»). Итог: **pytest 460/460 ✅, смоук 9 страниц 200 + 7 джобов ✅, data/ чистая**. Нюанс: при параллельном прогоне pytest одним агентом во время правок второго — ложные падения (KeyError 'token'); верить только контрольному прогону оркестратора после обеих волн. Дальше — деплой на mako-play (пользователь) и приёмка на реальном LLM.

- **2026-06-10 (7)** — **ЭТАП 7 «ЗАВОД 2.0», ВОЛНЫ A–C** (запрос пользователя: редизайн воркфлоу — полуавтономный цикл вместо ручного «идеи→пост→ревью»). Оркестратор: спецификация+контракты в PLAN.md → Этап 7, заглушки 4 web-роутеров + main.py, джобы process_brain/process_factory в scheduler.py. Волна A (1 sonnet): catalog.py, БД-миграции (strategies, plan_items, stage/settings/content_types, пересборка content), 6 FAKE-purposes. Волна B (2 sonnet ∥ 1 opus): ИИ-профиль при создании проекта, панель запуска (типы контента, горизонты, «В работу», паузы), пайплайн стратегии с версионированием/пересмотром + UI. Волна C (2 sonnet ∥ 1 opus): планнер+brain (rolling-план), from_plan+factory (генерация одобренного: post/story/video, авто-schedule), шахматка /plan. Итог: **pytest 388/388 ✅, смоук 8 страниц 200 + 7 джобов ✅, data/ чистая**. Инцидент: session limit срезал отчёты 2 агентов волны C — работа оказалась дописанной (см. Нюансы). Осталась ВОЛНА D (очередь готового /queue, актуализация площадок, retention per project, e2e нового флоу, DEPLOY.md).

- **2026-06-10 (6)** — **ЭТАП 5 (АНАЛИТИКА И САМООБУЧЕНИЕ) ЗАВЕРШЁН, ВОЛНА 7** (оркестратор + 4 сабагента: collector [S] ∥ дашборд /analytics [S] ∥ analyzer [O] ∥ A/B-хуки [S]; оркестратор сам: UNIQUE-индекс metrics до агентов, джобы scheduler.py после). Итог: сбор метрик VK/YouTube daily, /analytics с Chart.js и ручным вводом, LLM-анализ → learnings weekly, A/B-посты. pytest 197/197. **Реализация завода завершена (этапы 0–5).** Затем — прод-инцидент: завод на mako-play получал 404 от роутера; корень — systemd EnvironmentFile не режет inline-комментарии в .env (см. Нюансы). Фикс: харденинг config.py (_SINGLE_TOKEN_FIELDS, 203/203 тестов) + оба DEPLOY.md переписаны под конкретные значения сервера (без сессионных переменных, юнит без EnvironmentFile). Пользователю выданы команды починки сервера.
- **2026-06-10 (5)** — **ЭТАП 4 (ПУБЛИКАЦИЯ ВИДЕО) ЗАВЕРШЁН в dry-run, ВОЛНА 6** (оркестратор + 4 сабагента: VK/TG-видео [S] ∥ YouTube [S] ∥ manual-UI [H], затем e2e-сценарий D [S]). Оркестратор заранее зафиксировал стыки сам: base.py (PublishDeferred, ветка youtube, files→vk) и scheduling.py (перенос без сжигания attempts). Итог: TG sendVideo, VK video.save+wallpost (клипы недоступны — партнёрский метод), YouTube Shorts через чистый httpx с квотой 6/день и переносом по PublishDeferred, IG в ручной очереди с плеером и «Скачать mp4». **pytest 153/153 ✅, смоук 5 страниц 200 ✅, data/ чистая.** Реальные ключи пользователь не дал (нет времени) — приёмка с живыми токенами отложена. Дальше — этап 5 (аналитика, collector с dry-run + ручной ввод метрик).
- **2026-06-10 (1)** — проектирование системы, этап 0: PLAN/STATE/CLAUDE/agents. DeepSeek-ключ в `.env`, `.gitignore`, усилен протокол (обновление STATE/PLAN после каждого цикла).
- **2026-06-10 (2)** — решение пользователя: схема «оркестратор + сабагенты» вместо переключения модели главной сессии (поле model из settings.json убрано). Создан и протестирован Stop-хук свежести STATE.md (пропуск/блок/защита от цикла — все пути проверены; подхватится после перезапуска сессии или /hooks). **Этап 1: LLM-Router построен двумя sonnet-сабагентами и проверен оркестратором реальными вызовами DeepSeek.** Найден и исправлен баг: эндпоинт всегда отдавал SSE → теперь non-stream возвращает единый JSON, стрим не буферируется, токены пишутся. Веб-кабинет: логин, аккаунты (free выделены), ключи, статистика — все проверки прошли. Остался деплой на VPS.
- **2026-06-10 (4)** — **ЭТАП 3 (ВИДЕОКОНВЕЙЕР) ЗАВЕРШЁН, ВОЛНА 5** (оркестратор + 4 сабагента: tts/субтитры [S] ∥ ассеты/ротация [S], затем рендер [O] ∥ склейка+веб [S]). Оркестратор заранее развёл конфликты: config/.env.example/requirements правил сам до запуска параллельных агентов; стык produce↔render через ленивый импорт. Итог: полный конвейер идея → видео-сценарий → TTS → субтитры → ассеты → рендер → ревью с превью. pytest 132/132 ✅, сквозная интеграция с настоящим ffmpeg ✅ (оба шаблона, 1080x1920). Дальше — этап 4 (публикация видео), перед ним глазами проверить реальный ролик (нужны бесплатные PEXELS/PIXABAY ключи).
- **2026-06-10 (3)** — **ЭТАП 2 ПОЛНОСТЬЮ ЗАВЕРШЁН** (оркестратор + 6 sonnet-сабагентов, 4 волны). (а) `LLMRouter\DEPLOY.md` — копипаст-деплой роутера (systemd + поддомен в существующий Caddyfile + обновление rsync). (б) Перепроектирование по требованию пользователя: проекты динамические в БД с CRUD через дашборд, projects.yaml отменён, per-project креденшелы Fernet. (в) Волна 1: скелет, 7 таблиц, CRUD проектов/площадок, llm.py+FAKE_LLM. (г) Волна 2 (2 параллельных агента): pipeline ideas/script + промпты ideas.txt/script.txt; publishers TG/VK/manual с dry-run в data/outbox + scheduler. Интеграционный фикс стыка волн: Settings → динамическое чтение env (5 падений устранены в корне). (д) Волна 3: ревью-очередь с правкой и approve+планированием, reject→learnings, календарь месяц/неделя с переносом/retry, ручная очередь, сводка на главной. (е) Волна 4: e2e A/B/C (полный цикл по HTTP, петля обучения reject→промпт, отказоустойчивость 3 ретрая→error→retry) + `DEPLOY.md` завода (порт 8200, basic_auth через Caddy — у дашборда нет своего логина!). Финальная верификация оркестратора: **pytest 64/64 ✅, смоук 5 страниц 200 ✅, продовые data/ чистые**. Нюанс волны 4: `from app.llm import chat` в ideas.py → в тестах патчить `app.pipeline.ideas.chat`, не `app.llm.chat`.
